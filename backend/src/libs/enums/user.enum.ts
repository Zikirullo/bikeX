export enum UserType {
  ADMIN = "ADMIN",
  USER = "USER",
}

export enum UserStatus {
  ACTIVE = "ACTIVE",
  BLOCK = "BLOCK",
  DELETED = "DELETED",
}

export enum UserAuth {
  PHONE = "PHONE",
  EMAIL = "EMAIL",
}
